{
  "name": "alicia",
  "version": "1.0.0",
  // ... (bagian lain)
  "scripts": {
    "start": "node index.js"
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  // ... (bagian lain)
  "dependencies": {
    // Pastikan express ada di sini
    "express": "^4.19.2",  
    "discord.js": "^14.25.1",
  }
}